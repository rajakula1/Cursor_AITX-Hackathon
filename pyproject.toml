[project]
name = "pa-intake-agent"
version = "0.1.0"
description = "FieldCheck Prior Authorization — AITX Hackathon"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
testpaths = ["tests"]
